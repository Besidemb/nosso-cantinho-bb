<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nosso Cantinho 💜</title>
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: radial-gradient(circle at top left, #4e1b6f, #2c1240);
            color: #f5f0ff;
            margin: 0;
            padding: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
        }

        h1 {
            margin-top: 30px;
            text-shadow: 0 0 15px #b17aff, 0 0 30px #7e52a0;
            font-size: 2.8rem;
        }

        .container {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 40px;
            margin-top: 40px;
        }

        .perfil {
            background-color: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(12px);
            border-radius: 15px;
            padding: 25px;
            width: 300px;
            box-shadow: 0 0 25px rgba(255, 255, 255, 0.2);
            text-align: center;
        }

        .perfil h2 {
            color: #e2c8ff;
            margin-bottom: 15px;
            text-shadow: 0 0 10px #9b70d1;
        }

        .notas {
            background-color: rgba(0, 0, 0, 0.4);
            color: #fff;
            padding: 10px;
            border-radius: 8px;
            min-height: 100px;
            text-align: left;
            white-space: pre-wrap;
            overflow-wrap: break-word;
            margin-bottom: 10px;
        }

        .btn {
            background-color: #d0b8ff;
            border: none;
            color: #2c1240;
            padding: 8px 15px;
            margin: 5px;
            border-radius: 6px;
            cursor: pointer;
            transition: 0.3s;
        }

        .btn:hover {
            background-color: #b89cf7;
        }

        .moedas {
            margin-top: 15px;
        }

        footer {
            margin-top: 50px;
            padding: 20px;
            font-size: 0.9rem;
            color: #d9c2ff;
        }
    </style>
</head>
<body>
    <h1>Nosso Cantinho 💜</h1>
    <div class="container">
        <div class="perfil">
            <h2>MB</h2>
            <div class="notas" id="notas-mb" contenteditable="true" placeholder="Escreva algo..."></div>
            <div class="moedas">
                <p>Moedas: <span id="moedas-mb">100</span></p>
                <button class="btn" onclick="alterarMoedas('mb', 10)">+10</button>
                <button class="btn" onclick="alterarMoedas('mb', -10)">-10</button>
            </div>
        </div>

        <div class="perfil">
            <h2>Nath</h2>
            <div class="notas" id="notas-nath" contenteditable="true" placeholder="Escreva algo..."></div>
            <div class="moedas">
                <p>Moedas: <span id="moedas-nath">100</span></p>
                <button class="btn" onclick="alterarMoedas('nath', 10)">+10</button>
                <button class="btn" onclick="alterarMoedas('nath', -10)">-10</button>
            </div>
        </div>
    </div>

    <footer>Feito com 💜 por MB & Nath</footer>

    <script>
        // Atualiza moedas
        function alterarMoedas(perfil, valor) {
            const span = document.getElementById(`moedas-${perfil}`);
            let total = parseInt(span.textContent);
            total = Math.max(0, total + valor);
            span.textContent = total;
            salvarDados();
        }

        // Salva notas e moedas no navegador
        function salvarDados() {
            const dados = {
                mb: {
                    notas: document.getElementById('notas-mb').innerText,
                    moedas: document.getElementById('moedas-mb').textContent
                },
                nath: {
                    notas: document.getElementById('notas-nath').innerText,
                    moedas: document.getElementById('moedas-nath').textContent
                }
            };
            localStorage.setItem('nossoCantinho', JSON.stringify(dados));
        }

        // Carrega dados salvos
        window.onload = function() {
            const salvos = localStorage.getItem('nossoCantinho');
            if (salvos) {
                const dados = JSON.parse(salvos);
                document.getElementById('notas-mb').innerText = dados.mb.notas;
                document.getElementById('moedas-mb').textContent = dados.mb.moedas;
                document.getElementById('notas-nath').innerText = dados.nath.notas;
                document.getElementById('moedas-nath').textContent = dados.nath.moedas;
            }
        };

        // Salva automaticamente quando sai da página
        window.addEventListener('beforeunload', salvarDados);
    </script>
</body>
</html>
